language: java
jdk: oraclejdk8
dist: trusty
cache:
  directories:
  - $HOME/.m2
sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
before_install: cd singa-all
script: mvn clean install -X -Xdiags:verbose